{
	"dependencies": {
		"@tauri-apps/api": "^2.9.0",
		"@tauri-apps/plugin-autostart": "^2.5.1",
		"@tauri-apps/plugin-notification": "^2.3.3"
	},
	"devDependencies": {
		"vite": "^5.0.0"
	},
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"tauri:dev": "pnpm run dev & cargo tauri dev",
		"tauri:build": "pnpm run build && cargo tauri build",
		"clean": "node clean.js"
	}
}